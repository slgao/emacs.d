** initialize emacs config
#+BEGIN_SRC emacs-lisp
(package-initialize)
#+END_SRC
** suppress deprecated cl package warnings from old third-party packages
#+BEGIN_SRC emacs-lisp
(setq byte-compile-warnings '(not cl-functions))
#+END_SRC
** integrate emacs config from different modules
#+BEGIN_SRC emacs-lisp
  (add-to-list 'load-path "~/.emacs.d/lisp")
  (add-to-list 'load-path "~/.emacs.d/site-lisp")
  ;; also include subdirectories (let-bind default-directory instead of cd,
  ;; which would permanently change Emacs's startup working directory)
  (let ((default-directory "~/.emacs.d/site-lisp"))
    (normal-top-level-add-subdirs-to-load-path))

  (defun open-my-init-file()
    (interactive)
    (find-file "~/.emacs.d/init.el"))

  ;; emacs init files
  (my/load-org-config "lisp/init-utils.org")
  (require 'init-packages)
  (require 'init-markdown)
  (require 'init-ui)
  (require 'init-org)
  (require 'init-keybindings)
  (require 'browse-kill-ring)
  (require 'init-better-defaults)
  (require 'flycheck-virtualenv)

  (setq custom-file (expand-file-name "lisp/custom.el" user-emacs-directory))

  (load-file custom-file)

#+END_SRC
